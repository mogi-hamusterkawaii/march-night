rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DENY ALL: System migrated to Supabase PostgreSQL.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
